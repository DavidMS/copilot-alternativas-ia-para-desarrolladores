# El Rol de la IA en el desarrollo profesional con Spring

## 1. Evolución del desarrollo asistido por IA y "Agentic Development"

No estamos ante una mejor versión de Google o ante un autocompletar con esteroides. Estamos ante un cambio de paradigma en la relación humano-máquina donde **la abstracción sube de nivel**.

La integración de la IA en el desarrollo de software no ha sido un salto repentino, sino una **evolución gradual** en tres eras claramente diferenciadas. Cada era ha incrementado la **autonomía** de la IA y ha cambiado el **rol del desarrollador**.

```
Desarrollador escribe todo → IA sugiere líneas → IA conversa → IA ejecuta y actúa
         (2020)                   (2021)           (2023)         (2025+)
```

---

### Era 1: Autocomplete — "Ghost Text" (2021-2022)

#### ¿Qué es?
El **Autocomplete** (popularizado por GitHub Copilot) es un modelo de IA que predice las **siguientes líneas de código** basándose en el contexto inmediato del archivo abierto.

#### Características
| Aspecto | Detalle |
|---|---|
| **Input** | Las líneas anteriores y el nombre del archivo |
| **Output** | Una o varias líneas de código sugeridas ("ghost text") |
| **Contexto** | Limitado al archivo actual y, como mucho, archivos abiertos en tabs |
| **Acción** | Ninguna. Solo sugiere, tú aceptas o rechazas |

#### Ejemplo Real: Autocompletado en Spring Boot

Imagina que estás en un archivo `UserService.java` y escribes:

```java
// Método para buscar un usuario por email
public Optional<User> findByEmail(
```

El autocomplete sugeriría algo como:

```java
public Optional<User> findByEmail(String email) {
    return userRepository.findByEmail(email);
}
```

**¿Está bien?** A veces sí. Pero el autocomplete:
- ❌ No sabe si `userRepository` tiene ese método.
- ❌ No sabe si el tipo de retorno debería ser `Optional` o lanzar una excepción.
- ❌ No puede verificar que compila.
- ❌ No puede ejecutar los tests para ver si rompe algo.

#### Limitaciones Clave
1. **Sin contexto global**: No entiende la arquitectura de tu proyecto.
2. **Sin ejecución**: No puede correr `mvn compile` para verificar su sugerencia.
3. **Sin memoria**: Cada sugerencia es independiente; no recuerda lo que sugirió antes.
4. **Sesgo hacia lo popular**: Tiende a generar código "promedio" de Stack Overflow, no código adaptado a TU proyecto.

#### Mentalidad del Desarrollador en esta Era

> "Ayúdame a escribir este método más rápido."

El desarrollador sigue siendo el **conductor**. La IA es un **copiloto pasivo** que solo completa frases.

---

### Era 2: Chat — Razonamiento Conversacional (2023-2024)

#### ¿Qué es?
Los modelos conversacionales como ChatGPT, Claude Web o Copilot Chat permiten hacer **preguntas en lenguaje natural** y recibir explicaciones, código, y razonamiento detallado.

#### Características
| Aspecto | Detalle |
|---|---|
| **Input** | Pregunta en lenguaje natural + código copiado manualmente |
| **Output** | Explicaciones, bloques de código, alternativas |
| **Contexto** | Solo lo que tú le pegas en el chat |
| **Acción** | Ninguna. Tú copias y pegas el resultado |

#### Ejemplo Real: Pidiendo Código por Chat

Tú preguntas en el chat:

> "Necesito un endpoint REST en Spring Boot que reciba un JSON con `name` y `email`, valide los campos, y guarde un usuario en la base de datos. Usa arquitectura hexagonal."

La IA responde con un bloque de código largo:

```java
// UserController.java
@RestController
@RequestMapping("/api/users")
public class UserController {

    private final CreateUserUseCase createUserUseCase;

    public UserController(CreateUserUseCase createUserUseCase) {
        this.createUserUseCase = createUserUseCase;
    }

    @PostMapping
    public ResponseEntity<UserResponse> createUser(
            @Valid @RequestBody CreateUserRequest request) {
        User user = createUserUseCase.execute(
            new CreateUserCommand(request.name(), request.email())
        );
        return ResponseEntity.status(HttpStatus.CREATED)
            .body(UserResponse.from(user));
    }
}
```

```java
// CreateUserUseCase.java (Puerto de Entrada)
public interface CreateUserUseCase {
    User execute(CreateUserCommand command);
}
```

```java
// CreateUserService.java (Implementación del caso de uso)
@Service
public class CreateUserService implements CreateUserUseCase {

    private final UserRepository userRepository;

    public CreateUserService(UserRepository userRepository) {
        this.userRepository = userRepository;
    }

    @Override
    public User execute(CreateUserCommand command) {
        // Lógica de negocio
        if (userRepository.existsByEmail(command.email())) {
            throw new EmailAlreadyExistsException(command.email());
        }
        User user = User.create(command.name(), command.email());
        return userRepository.save(user);
    }
}
```

#### El Problema: "The Copy-Paste Gap"

Este código puede estar **muy bien**, pero:

1. **Tú** tienes que copiar cada bloque al archivo correcto.
2. **Tú** tienes que crear las clases que faltan (`CreateUserCommand`, `UserResponse`, `User`, etc.).
3. **Tú** tienes que verificar que compila.
4. **Tú** tienes que correr los tests.
5. Si algo no funciona, tienes que **volver al chat**, pegar el error, esperar otra respuesta, y repetir.

```
┌──────────────────────────────────────────────────────────┐
│                    THE COPY-PASTE GAP                     │
│                                                          │
│  Navegador (Chat)  ──copy──>  IDE  ──paste──>  Terminal  │
│       │                                          │       │
│       │       Error! ← ← ← ← ← ← ← ← ← ← ← ←│       │
│       │                                                  │
│       └────── copy error, paste back to chat ────────────│
│                                                          │
│                    💀 CICLO INFINITO                       │
└──────────────────────────────────────────────────────────┘
```

#### Limitaciones Clave
1. **Sin acceso a tu proyecto**: No puede leer tu `pom.xml`, tu estructura de paquetes, ni tus convenciones.
2. **Sin ejecución**: No puede ejecutar `mvn test` ni ver si su código compila.
3. **Pérdida de contexto**: Si la conversación se alarga, olvida lo que dijo al principio.
4. **El humano es el integrador**: Tú eres el "pegamento" entre el código generado y tu proyecto real.

#### Mentalidad del Desarrollador en esta Era

> "Explícame cómo hacer esto y dame el código para copiar."

El desarrollador es un **integrador manual**. La IA es un **consultor externo** que da consejos pero no se ensucia las manos.

---

### Era 3: Agentes — Ejecución Autónoma (2025+)

#### ¿Qué es?
Un **Agente de IA** es un modelo que no solo genera código, sino que **opera directamente sobre tu proyecto**: lee archivos, crea carpetas, edita código, ejecuta comandos en terminal, interpreta errores, y corrige iterativamente.

#### Características
| Aspecto | Detalle |
|---|---|
| **Input** | Instrucciones en lenguaje natural (prompts) |
| **Output** | Archivos creados/modificados, comandos ejecutados, logs de resultados |
| **Contexto** | Todo tu sistema de archivos, terminal, y herramientas CLI |
| **Acción** | **Directa**. Ejecuta, verifica, y corrige |

#### Herramientas que Definen esta Era

| Herramienta | Tipo | Descripción |
|---|---|---|
| **Claude Code** | CLI Agent | Opera en terminal, acceso total a archivos y comandos |
| **Copilot Agent Mode** | IDE Agent | Integrado en VS Code, ejecuta acciones dentro del editor |
| **Cursor / Windsurf** | IDE Agent | IDEs con agentes nativos integrados |
| **Aider** | CLI Agent | Agente CLI open-source para Git |
| **Cline / Roo Code** | IDE Extension | Extensiones que convierten VS Code en un entorno agéntico |

#### Ejemplo Real: El Mismo Caso, con un Agente

En lugar de copiar y pegar, le dices al agente directamente en terminal:

```bash
# Prompt al agente (en tu terminal, dentro de tu proyecto)
> Crea un endpoint POST /api/users que reciba name y email,
  valide los campos, y guarde un usuario en la base de datos.
  Usa arquitectura hexagonal siguiendo la estructura existente.
  Escribe tests unitarios y de integración.
  Ejecuta los tests y asegúrate de que todos pasan.
```

**Lo que el agente hace automáticamente:**

```
1. 🔍 Lee tu proyecto
   - Analiza pom.xml → Detecta Spring Boot 3.4, Java 21
   - Analiza src/main/ → Detecta estructura hexagonal existente
   - Analiza tests existentes → Detecta patrón de testing

2. ⌨️ Crea archivos
   - src/main/java/.../adapter/in/web/UserController.java
   - src/main/java/.../application/port/in/CreateUserUseCase.java
   - src/main/java/.../application/service/CreateUserService.java
   - src/main/java/.../domain/model/User.java
   - src/main/java/.../adapter/out/persistence/UserJpaEntity.java
   - (... y todos los archivos necesarios)

3. 🖥️ Ejecuta
   $ mvn compile → ✅ BUILD SUCCESS
   $ mvn test    → ❌ FAILURE: UserRepositoryTest - table 'users' not found

4. 🔄 Corrige
   - Lee el error → Falta el script de migración Flyway
   - Crea: src/main/resources/db/migration/V2__create_users_table.sql
   $ mvn test    → ✅ BUILD SUCCESS (12 tests passed)

5. ✅ Reporta
   "He creado el módulo de usuarios con 8 archivos.
    12 tests pasan (4 unitarios, 4 integración, 4 controller).
    ¿Quieres que revise algo?"
```

#### El Cambio Fundamental

```
┌──────────────────────────────────────────────────────┐
│              ANTES (Chat)          AHORA (Agente)     │
│                                                      │
│  Tú escribes código       Tú escribes especificación │
│  Tú copias y pegas        El agente implementa       │
│  Tú compilas              El agente compila           │
│  Tú debuggeas             El agente debuggea          │
│  Tú corres tests          El agente corre tests       │
│  Tú eres el obrero        Tú eres el arquitecto       │
│                                                      │
│  Tu rol:  IMPLEMENTAR  →  Tu rol:  REVISAR y GUIAR   │
└──────────────────────────────────────────────────────┘
```

#### Mentalidad del Desarrollador en esta Era

> "Aquí tienes la especificación. Implementa el módulo y avísame cuando los tests pasen."

El desarrollador es un **arquitecto y revisor**. La IA es un **desarrollador junior incansable** que ejecuta las instrucciones.

---

### Comparativa Directa: Las Tres Eras

| Dimensión | Autocomplete | Chat | Agente |
|---|---|---|---|
| **Metáfora** | Predictive text del móvil | Consultor externo | Junior developer |
| **Contexto** | Archivo actual | Lo que le pegas | Todo el proyecto |
| **Acción** | Sugerir texto | Generar bloques | Ejecutar y verificar |
| **Loop de feedback** | Manual | Copy-paste | Automático |
| **Escalabilidad** | 1 línea | 1 conversación | 1 feature completa |
| **Riesgo** | Bajo (aceptas o no) | Medio (pegaste mal) | Alto (si no especificas bien) |
| **Skill requerido** | Saber escribir código | Saber hacer prompts | Saber **especificar** |

---

#### 🔍 Referencias de Utilidad
* **Tweets de Karpathy sobre Vibe Coding:** [Ref 1](https://x.com/karpathy/status/1886192184808149383), [Ref 2](https://x.com/karpathy/status/2019137879310836075).
* **The Rise of AI Agents (Andrew Ng):** [YouTube](https://www.youtube.com/watch?v=KrRD7r7y7NY).
* **Copilot Instructions:** `.github/copilot-instructions.md` para estandarizar el estilo del equipo.

---

## 2. Qué problemas reales resuelve en backend y microservicios

En el desarrollo de backend con Spring, el problema no es "escribir código", sino gestionar la **complejidad de las abstracciones** y la **coherencia del ecosistema**.

---

#### 2.1. Gestión de la Carga Cognitiva en Arquitecturas Distribuidas
* **Contextualización rápida:** Uso de la IA para explicar flujos de peticiones entre servicios, interceptores y trazabilidad.
* **Identificación de dependencias:** Resolución de conflictos en el classpath (`pom.xml` / `build.gradle`).

#### 2.2. Automatización de la "Burocracia del Framework"
* **Mapeos Complejos:** Implementación de mappers (MapStruct) entre JPA, Dominio y DTOs.
* **Spring Security 6.x:** Generación de `SecurityFilterChain` sin memorizar APIs fluidas cambiantes.

#### 2.3. Modernización y Refactorización de Legacy
* **Migraciones:** Automatización de Java 8 a 21 y Spring Boot 2.x a 3.x (Jakarta EE).
* **Sustitución de API:** De `RestTemplate` a `RestClient` o `WebClient`.

#### 2.4. Estrategias de Testing de Integración
* **Infraestructura:** Generación de código para **TestContainers** y mocks con **WireMock**.
* **Contratos:** Creación de tests de Spring Cloud Contract o Pact.

---

## 🧠 Reflexión Clave

> **"El valor de un desarrollador senior ya no está solo en cuánto código sabe escribir, sino en cuán precisamente sabe especificar lo que quiere."**

La transición del Chat al Agente no es un cambio de herramienta; es un **cambio de rol**:

- **Antes**: Eras un **artesano** que esculpía código línea a línea.
- **Ahora**: Eres un **director de orquesta** que define QUÉ tocar y revisa CÓMO suena.

Esto tiene implicaciones profundas que exploraremos en los siguientes temas:

1. Si la IA ejecuta, ¿qué necesita para hacerlo bien? → **Un buen entorno** (Tema 2)
2. ¿Cómo se ve en la práctica? → **Un ejemplo real** (Tema 3)
3. ¿Cómo evitamos que "alucine"? → **Especificaciones formales (SDD)** (Tema 4)

---

## 💡 Ejercicio Rápido (10 min)

Clasifica cada una de estas acciones en la era a la que pertenecen:

1. Pegar un error de compilación en ChatGPT y pedir la solución.
2. Escribir `// sort this list` y que aparezca `Collections.sort(list)`.
3. Decir "Crea un módulo de autenticación con JWT y asegúrate de que los tests pasen".
4. Pedir a Claude Web que te explique qué hace un bloque de código legado.
5. Un agente que detecta que tu API no maneja errores 404 y añade el handler automáticamente.

<details>
<summary>Ver Respuestas</summary>

1. **Era 2 (Chat)** — Copy-paste gap clásico.
2. **Era 1 (Autocomplete)** — Sugerencia en línea.
3. **Era 3 (Agente)** — Delegación completa con verificación.
4. **Era 2 (Chat)** — Consulta conversacional.
5. **Era 3 (Agente)** — Detección proactiva y acción autónoma.

</details>

### 🛠 Material Sugerido para el Curso

#### 📊 Tabla: Impacto en Tareas de Senior Backend
| Tarea Crítica | Dolor Manual | Solución con IA | Valor para el Senior |
| :--- | :--- | :--- | :--- |
| **Setup de Seguridad** | Alta probabilidad de error | Generación basada en specs | Seguridad por diseño |
| **Testing de Integración** | Configuración pesada | Boilerplate de TestContainers | Mayor cobertura real |

#### 🧪 Ejercicio de Crítica: "El Detector de Deprecaciones"
> **Reto:** Actualizar un `WebSecurityConfigurerAdapter` antiguo a Spring Boot 3.2 y detectar si la IA "alucina" con métodos inexistentes.

### 🔍 Referencias de Utilidad
* **[Accelerate (Nicole Forsgren)](https://github.com/LuckyDudeThakur/EBooks/blob/master/Accelerate%20-%20Building%20and%20Scaling%20High%20Performing%20Technology%20Organisations%20-%20Nicole%20Fergrson.pdf):** Para entender cómo la mejora en las capacidades técnicas impacta en el rendimiento de la organización.
* **[The Pragmatic Programmer](https://github.com/lighthousand/books/blob/master/the-pragmatic-programmer.pdf):** Principios clásicos que siguen siendo la mejor defensa contra el uso descuidado de la tecnología.
* **[Spring AI Project](https://spring.io/projects/spring-ai):** Referencia para aquellos alumnos que quieran llevar el curso más allá e integrar IA directamente en sus aplicaciones Spring.


---

## 3. Productividad vs calidad: métricas útiles (lead time, defect rate, PR cycle time)

El error común en el management es medir la productividad por "líneas de código". Para un Senior de Spring, la IA no es una fábrica de líneas (lo que aumentaría la deuda técnica), sino un acelerador de la entrega de valor y un optimizador de la carga cognitiva.

---

#### 3.1. Métricas de Flujo (Velocidad de entrega)
* **PR Cycle Time (Tiempo de ciclo de Pull Request):** Es la métrica donde la IA tiene un impacto más inmediato.
    * **Fase de Autoría:** Reducción drástica del tiempo para generar el esqueleto de controladores, servicios y DTOs.
    * **Fase de Revisión:** Uso de la IA para resumir cambios complejos en PRs de compañeros, permitiendo que el Senior actúe como "Editor Jefe" de forma más ágil.
* **Lead Time for Changes:** Tiempo total desde que se inicia un commit hasta que llega a producción. Al automatizar la configuración de beans, integraciones y scripts de despliegue, el cuello de botella técnico desaparece.

#### 3.2. Métricas de Calidad (Estabilidad y Robustez)
* **Defect Rate (Tasa de defectos):** ¿La IA introduce más errores?
    * *Enfoque Senior:* La IA debe usarse para aumentar la **cobertura de tests** (unitarios, integración con TestContainers, tests de mutación). Si se usa para generar código sin tests, la tasa de defectos sube; si se usa para generar tests robustos, la tasa baja.
* **Mean Time to Recovery (MTTR):** Capacidad de usar la IA para diagnosticar fallos en producción analizando trazas de Stacktraces de Spring o logs de microservicios en segundos.

#### 3.3. La paradoja de la "Falsa Productividad" (Code Bloat)
* **Métrica de Densidad de Código:** Un Senior debe vigilar que la IA no genere 500 líneas para algo que se resuelve con una anotación de Spring o una librería existente. 
* **Eficiencia de Revisión:** Medir cuánto tiempo dedica el Senior a validar el código generado vs. escribirlo. Si la validación tarda más que la escritura manual, la herramienta está restando productividad.

---

### 🛠 Material Sugerido para el Curso

#### 📊 Gráfico: El nuevo reparto del tiempo del Senior

* **Antes:** 20% Diseño | 50% Coding | 30% Testing/Review.
* **Ahora con IA:** 40% Diseño y Especificación | 10% Coding (Generado) | 50% Validación, Seguridad y Refactorización.

#### 💡 Tabla: Métricas DORA bajo la influencia de la IA

| Métrica DORA | Impacto de la IA | Riesgo para el Senior |
| :--- | :--- | :--- |
| **Deployment Frequency** | **Aumenta:** Menos fricción en el setup de nuevos microservicios. | Generar "basura" técnica más rápido. |
| **Lead Time for Changes** | **Disminuye:** El boilerplate de Spring ya no es un impedimento. | Desconexión del dev con la arquitectura base. |
| **Change Failure Rate** | **Baja (potencialmente):** IA facilita la creación de edge cases en tests. | Confianza ciega en tests generados por la IA que "siempre pasan". |
| **Mean Time to Recovery** | **Disminuye:** IA acelera el diagnóstico de errores y sugiere fixes en contexto. | Aplicar soluciones sugeridas sin entender la causa raíz del incidente. |

#### 🧪 Ejercicio: "Análisis de una PR asistida"
> **Reto:** Se entrega a los alumnos una PR que contiene un error sutil: un `@Service` de Spring con un `@Scope("prototype")` que inyecta un bean singleton de forma incorrecta (un error clásico de hilos).
> **Misión:** > 1. Pedir a Copilot que explique la PR.
> 2. Preguntar a la IA: "¿Hay algún problema de concurrencia o de ciclo de vida de beans en este código?".
> 3. Documentar si la IA detectó el error o si intentó justificarlo.

---

### 🔍 Referencias de Utilidad
* **DORA Metrics (Google Cloud):** Estándar de la industria para medir equipos de alto rendimiento.
* **The SPACE Framework (Microsoft Research):** Cómo medir la satisfacción y productividad más allá de los commits.
* **Effective Java (Joshua Bloch):** Referencia para validar si los patrones sugeridos por la IA son realmente "Clean Code" en el ecosistema Java.

## 4. Qué no debe delegarse a la IA (decisiones de dominio, riesgo, seguridad, compliance)

Para un Senior de Spring, la IA es un motor de ejecución, pero nunca el juez de la arquitectura. Delegar ciegamente el "qué" y el "por qué" (frente al "cómo") puede comprometer la integridad del sistema a largo plazo.

---

#### 4.1. El Diseño del Modelo de Dominio (Domain-Driven Design)
* **El contexto del negocio:** La IA no conoce la evolución futura de tu producto ni los matices del lenguaje ubicuo de tu empresa.
* **Límites de los Bounded Contexts:** Decidir si dos servicios deben compartir una entidad o si deben estar desacoplados es una decisión estratégica que la IA tiende a simplificar erróneamente.
* **Invariantes de negocio:** Las reglas que aseguran que los datos son consistentes según la lógica de negocio deben ser definidas y supervisadas por el humano.

#### 4.2. Arquitectura de Seguridad y Criptografía
* **Gestión de Secretos:** Nunca permitas que la IA sugiera dónde o cómo guardar claves API o certificados (evitar `application.properties` en texto plano).
* **Protocolos de Auth:** La IA puede sugerir configuraciones de OAuth2 o JWT funcionales pero inseguras (ej. algoritmos de firma débiles o falta de validación de claims).
* **Modelado de Amenazas:** La identificación de vectores de ataque específicos en tu infraestructura es una tarea puramente humana.

#### 4.3. Compliance, Privacidad y Aspectos Legales
* **GDPR/RGPD:** La IA no sabe qué campos en tu entidad `User` son datos de carácter personal que requieren cifrado en reposo o políticas de borrado.
* **Auditoría:** La lógica de trazabilidad y auditoría legal (quién hizo qué y cuándo) debe ser diseñada para cumplir con normativas específicas (fintech, salud, etc.).

#### 4.4. Decisiones de Riesgo Tecnológico
* **Selección de Stack:** La IA suele estar sesgada hacia lo más popular en su dataset de entrenamiento, no hacia lo más estable o lo que mejor encaja en tu infraestructura actual.
* **Trade-offs de Rendimiento:** La IA puede generar un código "limpio" pero con problemas de N+1 en JPA que solo un Senior detecta al analizar el volumen de datos real.

---

### 🛠 Material Sugerido para el Curso

#### 📊 Gráfico: El Círculo de Control del Senior

* **Humano (Centro):** Estrategia, Seguridad, Dominio, Ética.
* **IA (Periferia):** Sintaxis, Boilerplate, Refactorización local, Sugerencia de tests.

#### 💡 Tabla: El Semáforo de la Delegación

| Área | ¿Delegable? | Razón |
| :--- | :---: | :--- |
| **Escritura de DTOs/Mappers** | ✅ Verde | Tarea mecánica, bajo riesgo lógico. |
| **Configuración de Persistencia** | ⚠️ Ámbar | Riesgo de eficiencia (Lazy vs Eager, Índices). |
| **Lógica de Precios/Financiera** | ❌ Rojo | Un error de "coma flotante" o redondeo es responsabilidad legal del dev. |
| **Política de CORS/CSP** | ❌ Rojo | Puerta abierta a ataques XSS/CSRF si se configura "para que funcione". |

#### 🧪 Ejercicio: "El Arquitecto Crítico"
> **Reto:** Pedir a Copilot: *"Diseña la arquitectura de microservicios para un sistema de pagos internacionales cumpliendo con normativas bancarias"*.
> **Misión:** Analizar la respuesta e identificar al menos 3 omisiones críticas (ej: falta de idempotencia, gestión de colas de error DLQ, o cifrado de datos sensibles).
> **Objetivo:** Demostrar que la IA da soluciones de "superficie" pero ignora la "profundidad" técnica.

---

### 🔍 Referencias de Utilidad
* **OWASP Top 10:** Referencia obligatoria para auditar lo que la IA genera.
* **Domain-Driven Design (Eric Evans):** Recordatorio de por qué el modelo es el corazón del software.
* **Spring Security Reference:** Para verificar que las configuraciones generadas siguen las *best practices* actuales de la industria.

## 5. Riesgos de uso acrítico y "overtrust"

El "overtrust" (exceso de confianza) es el fenómeno donde un desarrollador Senior empieza a validar código por "vibe" en lugar de por ejecución o análisis. En Spring, donde muchas cosas ocurren mediante "magia" de anotaciones y proxys en tiempo de ejecución, este riesgo se multiplica.

---

### 📂 Esqueleto del Contenido

#### 5.1. El Sesgo de Automatización y la Ceguera del Revisor
* **Definición:** La tendencia a favorecer las sugerencias de un sistema automatizado sobre la propia vigilancia manual.
* **En Spring:** Aceptar una configuración de `@Async` o `@Transactional` sin verificar si el proxy de Spring realmente puede interceptar la llamada (ej. llamadas a métodos privados o internos dentro de la misma clase).

#### 5.2. Alucinaciones Técnicas Sutiles
* **Anotaciones Fantasma:** La IA puede inventar atributos en anotaciones de Spring que parecen lógicos pero no existen (ej. `readOnly = true` en una anotación que no lo soporta).
* **Dependencias Inexistentes:** Sugerencia de librerías que no están en el Maven Central o versiones de Starters que contienen vulnerabilidades conocidas.

#### 5.3. Erosión de la Agudeza Mental (Skill Decay)
* **La trampa del "funciona":** Si dejas que la IA resuelva siempre los problemas de concurrencia o de configuración de Beans, perderás la capacidad de debuguear el `ApplicationContext` cuando las cosas fallen en producción y no tengas la IA a mano.
* **Efecto Anclaje:** La primera solución que propone Copilot condiciona tu forma de resolver el problema, limitando tu creatividad arquitectónica.

#### 5.4. El Problema de la "Masa Crítica" de Código
* Generar código es barato; mantenerlo es caro. El uso acrítico lleva a un crecimiento explosivo de la base de código que nadie en el equipo entiende realmente de forma profunda.

---

### 🛠 Material Sugerido para el Curso

#### 📊 Gráfico: La Curva de Confianza vs. Calidad

* Un gráfico que muestra cómo, a medida que aumenta la confianza ciega, la calidad del sistema tiende a caer debido a la acumulación de micro-errores no detectados.

#### 💡 Tabla: Errores comunes de "Overtrust" en Spring

| Sugerencia de la IA | Riesgo Oculto (Lo que el Senior debe ver) | Consecuencia |
| :--- | :--- | :--- |
| `@Transactional` en método privado | El proxy de Spring no lo interceptará. | La transacción no se ejecuta. |
| Query JPA con múltiples `JoinFetch` | Producto cartesiano en memoria. | Degradación crítica de performance. |
| Inyección de dependencias por campo | Dificulta los tests unitarios y rompe la inmutabilidad. | Código menos testeable y frágil. |
| `Optional.get()` sin chequeo | Presupone que el dato siempre existe. | `NoSuchElementException` en producción. |

#### 🧪 Ejercicio: "Encuentra la alucinación"
> **Reto:** Proporcionar un bloque de código generado por IA para un flujo de **Spring WebFlux**.
> **Misión:** El código parece perfecto, pero incluye un bloqueo de hilo (`.block()`) dentro de un operador reactivo o usa una librería no bloqueante de forma incorrecta.
> **Objetivo:** Que el alumno identifique por qué "funcionar en local" no significa que sea correcto para un entorno reactivo de alta carga.

---

### 🔍 Referencias de Utilidad
* **Cognitive Biases in Software Engineering:** Estudio de cómo los sesgos afectan a la revisión de código.
* **Spring Framework Issues (GitHub):** Para ver cómo errores reales de configuración a menudo coinciden con las "alucinaciones" más comunes de los LLMs.
* **The Ironies of Automation (Bainbridge):** Un clásico sobre cómo cuanto más eficiente es el sistema automático, más crucial (y difícil) es la intervención humana.

## 6. Cambios en el rol del desarrollador (más diseño, validación, revisión y automatización)

El rol del Senior está sufriendo una metamorfosis: de ser un "constructor de componentes" a ser un "director de sistemas". La habilidad de escribir sintaxis Java está perdiendo valor frente a la habilidad de orquestar flujos de trabajo y validar la integridad de sistemas complejos.

---

### 📂 Esqueleto del Contenido

#### 6.1. El Desarrollador como "Editor Jefe" (Reviewer-in-Chief)
* **De la creación a la curación:** La mayor parte del tiempo ya no se gasta frente a una hoja en blanco, sino revisando y refinando propuestas de la IA.
* **Agudeza en la lectura:** Leer código es ahora más importante que escribirlo. Se requiere una capacidad de detección de errores nivel "compilador humano".

#### 6.2. Énfasis en el Diseño de Sistemas y Contratos
* **Arquitectura sobre implementación:** El Senior debe dedicar más tiempo a definir cómo interactúan los microservicios (mensajería, eventos, APIs) que a escribir los controladores.
* **Pensamiento basado en Specs:** La habilidad principal pasa a ser la creación de especificaciones precisas (OpenAPI, AsyncAPI, esquemas de BD) que la IA pueda ejecutar sin ambigüedad.

#### 6.3. El Ingeniero de Automatización Integral
* **SDLC Automatizado:** El desarrollador ahora diseña el flujo que diseña el software. Esto incluye configurar agentes que no solo escriban código, sino que ejecuten tests, analicen seguridad y preparen el despliegue.
* **Prompt Engineering Técnico:** No se trata de "pedir cosas", sino de inyectar el contexto técnico necesario (Spring Context, perfiles de Maven, reglas de arquitectura) para que la IA sea productiva.

#### 6.4. Mentoring y Liderazgo en la era de la IA
* **Formar a Juniors:** El Senior debe enseñar a los perfiles menos experimentados a no depender ciegamente de la IA y a entender los fundamentos que la IA suele saltarse.

---

### 🛠 Material Sugerido para el Curso

#### 📊 Gráfico: La Pirámide de Valor del Desarrollador

* **Base (Valor en descenso):** Escritura de código repetitivo, boilerplate, mappers.
* **Medio (Valor estable):** Debugging complejo, optimización de rendimiento, tuning de Spring.
* **Cima (Valor en ascenso):** Diseño de arquitectura, seguridad, ética de IA, orquestación de agentes y visión de producto.

#### 💡 Tabla: Evolución de Tareas Diarias

| Tarea Tradicional | Nueva Tarea con IA | Habilidad Requerida |
| :--- | :--- | :--- |
| Escribir un `RestController` | Definir la spec OpenAPI y validar el contrato generado. | Diseño de APIs / Contratos. |
| Configurar manualmente un `KafkaListener` | Describir el flujo de eventos y supervisar la gestión de errores (DLQ). | Pensamiento sistémico. |
| Debuggear un `NullPointerException` | Pedir a la IA análisis de flujo y validar la lógica de protección sugerida. | Análisis crítico. |
| Implementar lógica de negocio | Escribir tests de aceptación que guíen a la IA en la implementación. | TDD / Spec Driven Development. |

#### 🧪 Ejercicio: "De Implementador a Arquitecto"
> **Reto:** Se da un requisito de negocio ambiguo: "Necesitamos un sistema de recompensas para usuarios que compran más de 3 veces al mes".
> **Misión:** > 1. En lugar de escribir el código, el alumno debe redactar la **especificación técnica** perfecta para que Copilot genere la solución completa (Entidades, Repositorios, Servicios y Tests).
> 2. Si el código generado falla o es incompleto, el alumno debe refinar la especificación, no el código manualmente.
> **Objetivo:** Practicar el liderazgo técnico sobre la herramienta.

---

### 🔍 Referencias de Utilidad
* **The End of Programming (Matt Welsh):** Una visión provocadora sobre cómo la programación pasará de ser una tarea lingüística a una tarea de ingeniería de control.
* **Staff Engineer (Will Larson):** Para entender cómo las responsabilidades de alto nivel (arquitectura, coordinación) se alinean con este nuevo rol asistido por IA.
* **Clean Architecture (Robert C. Martin):** Los principios de desacoplamiento son más vitales que nunca para que la IA pueda "entrar y salir" de los componentes sin romper el sistema.

## 7. Uso responsable: propiedad intelectual, privacidad y datos sensibles

El uso de IA en entornos corporativos (donde Spring es el estándar) no es solo una cuestión técnica, sino de cumplimiento legal y protección de activos. Un Senior debe liderar la política de "Higiene de Datos" al interactuar con LLMs.

---

### 📂 Esqueleto del Contenido

#### 7.1. Privacidad y Protección de Datos (GDPR/RGPD)
* **El riesgo de la fuga de datos:** Nunca incluir datos reales de clientes (PII - Personally Identifiable Information) en los prompts o en los archivos de contexto.
* **Sanitización de Logs:** Al pedir a la IA que analice un error de Spring, asegurarse de eliminar trazas de base de datos, emails o tokens de seguridad que aparezcan en los logs de la consola.

#### 7.2. Propiedad Intelectual y Licenciamiento
* **Código "Envenenado":** El riesgo de que la IA sugiera fragmentos de código protegidos por licencias restrictivas (como GPL) en un proyecto comercial cerrado.
* **Copyright del código generado:** Entender que, actualmente, en la mayoría de jurisdicciones, el código generado 100% por IA no es protegible por copyright; la autoría reside en la estructura y el diseño humano.

#### 7.3. Seguridad del Modelo (Prompt Injection y Leakage)
* **Fuga de secretos:** El peligro de tener archivos `.env` o `application-secret.yaml` abiertos en el IDE mientras Copilot está activo, ya que pueden ser enviados como contexto a los servidores de la IA.
* **Políticas de Retención:** Diferencia entre versiones "Free/Individual" (donde los datos pueden usarse para reentrenar modelos) y versiones "Enterprise" (con garantías de privacidad y no-entrenamiento).

#### 7.4. El concepto de "Responsabilidad Técnica"
* Si la IA sugiere una librería con una vulnerabilidad conocida (CVE), la responsabilidad legal y profesional recae en el desarrollador que aceptó el commit, no en el proveedor de la IA.

---

### 🛠 Material Sugerido para el Curso

#### 📊 Gráfico: El Flujo de Datos Seguro

* Un diagrama que muestre qué información **puede** salir hacia la IA (Estructura de clases, firmas de métodos, lógica genérica) y qué información **nunca** debe salir (Credenciales, Datos de clientes, Algoritmos de trading propietarios).

#### 💡 Tabla: Do's and Don'ts en el uso de Copilot

| Acción | Riesgo | Práctica Recomendada |
| :--- | :--- | :--- |
| **Pegar un JSON de respuesta de API** | Fuga de datos de producción. | Usar un JSON de ejemplo con datos ficticios (`John Doe`). |
| **Copiar configuración de Seguridad** | Exposición de estrategia de defensa. | Ofuscar nombres de endpoints críticos o roles sensibles. |
| **Indexar todo el repositorio** | Exposición de secretos en comentarios. | Usar archivos `.gitignore` y `.copilotignore` para excluir archivos sensibles. |
| **Generar lógica de cifrado** | Implementación de algoritmos débiles. | Usar estándares de la empresa y pedir a la IA que "use la clase X de nuestra librería". |

#### 🧪 Ejercicio: "Auditoría de Privacidad"
> **Reto:** Se proporciona un log de error de Spring Boot que incluye un volcado de memoria parcial con datos de usuario y una cadena de conexión a base de datos.
> **Misión:** El alumno debe "limpiar" el log antes de pasárselo a la IA para pedir una explicación del error.
> **Objetivo:** Crear el hábito de la sanitización proactiva de información.

---

### 🔍 Referencias de Utilidad
* **GitHub Copilot Trust Center:** Documentación oficial sobre cómo manejan los datos y la privacidad.
* **OWASP Top 10 for LLMs:** Riesgos específicos de seguridad al integrar o usar modelos de lenguaje.
* **ISO/IEC 42001:** El nuevo estándar internacional para la gestión de sistemas de Inteligencia Artificial.

## 8. Expectativas realistas: dónde acelera mucho y dónde no

Para un Senior de Spring, la IA no es una varita mágica, sino un multiplicador de fuerza. Sin embargo, un multiplicador por cero sigue siendo cero. Si no hay una base técnica sólida, la IA solo acelera la creación de desastres.

---

### 📂 Esqueleto del Contenido

#### 8.1. Zonas de Alta Aceleración (Quick Wins)
* **Boilerplate y Configuración:** Setup inicial de proyectos, anotaciones repetitivas, configuración de perfiles de Spring (`application-dev.yml`), y generación de Dockerfiles/K8s manifests.
* **Testing Unitario y de Integración:** Escritura de tests que siguen un patrón claro (AAA: Arrange-Act-Assert) y configuración de infraestructura de test (TestContainers).
* **Documentación y Explicación:** Generación de Javadoc, READMEs y explicaciones paso a paso de código complejo o legacy.
* **Conversión de Formatos:** Transformar un JSON en POJOs, o una tabla SQL en entidades JPA.

#### 8.2. Zonas de Baja Aceleración (Donde el Senior sigue solo)
* **Debugging de Problemas de Concurrencia:** Los errores de "race conditions" o deadlocks en entornos distribuidos suelen requerir un análisis de trazas que la IA no siempre puede conectar.
* **Tuning de Rendimiento Fino:** Decisiones sobre el tamaño del pool de conexiones (HikariCP) o la optimización de cachés de segundo nivel en Hibernate.
* **Arquitectura Evolutiva:** Decisiones sobre cuándo romper un monolito en microservicios basándose en la carga de trabajo y la estructura de los equipos (Ley de Conway).

#### 8.3. La Barrera del Contexto
* Aunque las ventanas de contexto han crecido, la IA aún tiene dificultades para "entender" las implicaciones de un cambio en un microservicio de la periferia sobre el núcleo del sistema si la base de código es masiva.

---

### 🛠 Material Sugerido para el Curso

#### 📊 Gráfico: Matriz de Eficiencia de la IA

* **Cuadrante 1 (Alta ayuda/Baja complejidad):** Boilerplate, Refactoring local.
* **Cuadrante 2 (Media ayuda/Alta complejidad):** Generación de nuevas features desde Specs.
* **Cuadrante 3 (Baja ayuda/Alta complejidad):** Debugging de sistemas distribuidos, Diseño de arquitectura.

#### 💡 Tabla: Realidad vs. Expectativa

| Lo que el Marketing dice | La Realidad del Senior de Spring |
| :--- | :--- |
| "La IA escribe aplicaciones completas" | La IA escribe componentes aislados que tú debes integrar. |
| "Ya no necesitas saber la sintaxis de Java" | Necesitas saber más que nunca para detectar errores sutiles. |
| "La IA arregla cualquier bug" | La IA te da pistas; tú eres el que tiene que leer el Heap Dump. |
| "Sustituirá a los programadores" | Sustituirá a los programadores que solo saben copiar de StackOverflow. |

#### 🧪 Ejercicio: "El Muro de la IA"
> **Reto:** Pedir a la IA que solucione un error de `LazyInitializationException` en una relación `@OneToMany` compleja con múltiples niveles de profundidad.
> **Misión:** Observar cómo la IA suele proponer la solución fácil pero incorrecta (ej: `enable_lazy_load_no_trans` o cambiar todo a `Eager`).
> **Objetivo:** Que el alumno identifique por qué la solución de la IA puede destrozar el rendimiento y deba aplicar la solución correcta (Entity Graphs o DTO Projections).

---

### 🔍 Referencias de Utilidad
* **The Mythical Man-Month (Fred Brooks):** Para recordar que añadir herramientas (o personas) no siempre reduce el tiempo de desarrollo de forma lineal.
* **State of AI 2025/2026 Reports:** Datos actualizados sobre dónde están encontrando valor real las empresas de desarrollo.
* **Spring Boot Performance Tuning Guide:** Como base para contrastar las sugerencias "genéricas" de la IA.

## 9. Enfoque del curso y criterios de éxito

Este curso no busca que los alumnos aprendan a "usar un chat", sino que evolucionen su flujo de trabajo profesional. El éxito no se mide por cuánto código genera la IA, sino por la calidad, seguridad y velocidad con la que el desarrollador Senior entrega soluciones complejas en Spring.

---

### 📂 Esqueleto del Contenido

#### 9.1. Metodología: "Hands-on with Guardrails"
* **Enfoque Práctico:** El curso se basa en proyectos de Spring Boot reales (microservicios, persistencia, seguridad).
* **El "Guardrail" (Barandilla):** Cada tarea generada con IA debe pasar por un proceso de revisión obligatoria basado en un checklist de calidad (Clean Code, Performance, Seguridad).

#### 9.2. Progresión del Aprendizaje
1. **Nivel 1 (Asistido):** Uso de Copilot para eliminar tareas mecánicas y boilerplate.
2. **Nivel 2 (Estratégico):** Uso de la IA para refactorización masiva y modernización de código legacy.
3. **Nivel 3 (Agéntico/SDD):** Definición de especificaciones técnicas (OpenAPI/Specs) y supervisión de la implementación completa por parte de la IA.

#### 9.3. Criterios de Éxito (KPIs del Alumno)
* **Autonomía Crítica:** El alumno identifica y corrige al menos el 90% de las alucinaciones o malas prácticas propuestas por la IA.
* **Reducción del Lead Time:** Capacidad de implementar una feature completa (desde el contrato hasta el test de integración) en un 40-60% menos de tiempo que el flujo tradicional.
* **Calidad del Código:** El código final debe cumplir con los estándares de producción (cobertura de tests >80%, sin vulnerabilidades críticas, y arquitectura desacoplada).

---

### 🛠 Material Sugerido para el Curso

#### 📊 Gráfico: El Radar de Competencias Final

* Un radar que muestre la evolución del alumno en:
    * **Prompting Técnico.**
    * **Revisión de Código.**
    * **Diseño de Arquitectura.**
    * **Gestión de Seguridad.**
    * **Velocidad de Entrega.**

#### 💡 Tabla: El "Definition of Done" del curso

| Objetivo | Evidencia de Éxito |
| :--- | :--- |
| **Dominio de IntelliJ + Copilot** | Uso fluido de atajos y paneles de chat para refactorizar beans de Spring. |
| **Spec Driven Development** | Implementación de un microservicio funcional a partir de un archivo YAML. |
| **Validación de Seguridad** | Identificación de configuraciones de Spring Security erróneas sugeridas por la IA. |
| **Infraestructura de Test** | Generación de una suite de tests de integración con TestContainers operativa. |

#### 🧪 Ejercicio Final: "The Senior Challenge"
> **Reto:** Se entrega un requisito de negocio complejo (ej: "Implementar un sistema de cuotas para una API con Redis y Spring Cloud Gateway").
> **Condición:** El alumno debe documentar el proceso:
> 1. Qué prompt/especificación inicial dio.
> 2. Qué errores detectó en la respuesta de la IA.
> 3. Cómo guió a la IA para llegar a la solución óptima.
> **Evaluación:** No se evalúa el código final, sino el **diario de decisiones** y las correcciones técnicas realizadas.

---

### 🔍 Referencias de Utilidad
* **Accelerate (Nicole Forsgren):** Para entender cómo la mejora en las capacidades técnicas impacta en el rendimiento de la organización.
* **The Pragmatic Programmer:** Principios clásicos que siguen siendo la mejor defensa contra el uso descuidado de la tecnología.
* **Spring AI Project:** Referencia para aquellos alumnos que quieran llevar el curso más allá e integrar IA directamente en sus aplicaciones Spring.